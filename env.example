NEXT_PUBLIC_APP_URL=http://localhost:3000
# Required: Base subgraph endpoint
NEXT_PUBLIC_SUBGRAPH_URL_BASE=https://api.thegraph.com/subgraphs/name/your-org/base-bridge
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_SENTRY_DSN=
NEXT_PUBLIC_MIXPANEL_TOKEN=

# Server-side (not exposed to client)
DATABASE_URL=postgresql://user:pass@localhost:5432/bridge_tracker
HELIUS_API_KEY=
HELIUS_WEBHOOK_SECRET=replace-with-strong-shared-secret
# Redis (optional - only needed for production with traditional Redis)
# REDIS_URL=redis://localhost:6379

# Upstash Redis (recommended for Vercel deployments)
# UPSTASH_REDIS_REST_URL=https://your-endpoint.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your-token-here

